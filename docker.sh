#!bin/bash
docker run -it -v /Users/felix/syntegris/Design_Patterns/js_design_patterns:/repo -p 9000:9000 gitpitch/desktop:pro

# or start the container via Docker Desktop -> Dashboard. Launch the web app via docker desktop, too.